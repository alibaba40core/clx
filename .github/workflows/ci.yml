name: CI

on:
  push:
    branches: [main, master, development]
  pull_request:
    branches: [main, master, development]

jobs:
  test:
    name: test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Build
        run: |
          go build -trimpath ./cmd/clx-ai ./cmd/clxmax
          go build -tags=lite -trimpath ./cmd/clx

      - name: Vet
        run: |
          go vet ./cmd/clx-ai ./cmd/clxmax ./...
          go vet -tags=lite ./cmd/clx/...

      - name: Test (race on Unix)
        if: runner.os != 'Windows'
        run: |
          go test -race ./...
          go test -tags=lite -race ./internal/pipeline/ ./internal/clxsidecar/ ./cmd/clx/

      - name: Test (Windows, no race — requires gcc for -race)
        if: runner.os == 'Windows'
        run: |
          go test ./...
          go test -tags=lite ./internal/pipeline/ ./internal/clxsidecar/ ./cmd/clx/

      - name: Build CLI
        shell: bash
        run: |
          mkdir -p bin
          go run ./cmd/genrules
          EXE="$(go env GOEXE)"
          go build -tags=lite -trimpath -ldflags="-s -w" -o "bin/clx${EXE}" ./cmd/clx
          go build -trimpath -ldflags="-s -w" -o "bin/clx-ai${EXE}" ./cmd/clx-ai
          go build -trimpath -ldflags="-s -w" -o "bin/clxmax${EXE}" ./cmd/clxmax

      - name: Check runtime budgets
        shell: bash
        run: bash scripts/check-budgets.sh

      - name: Version smoke test
        shell: bash
        env:
          CLX_HOME: ${{ runner.temp }}/clx-home
        run: |
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            ./bin/clx.exe --version
          else
            ./bin/clx --version
          fi

      - name: Doctor smoke test
        shell: bash
        env:
          CLX_HOME: ${{ runner.temp }}/clx-doctor
        run: |
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            ./bin/clx.exe doctor
          else
            ./bin/clx doctor
          fi
