intent: disk_usage
examples:
  - disk usage
  - how much disk space is free
  - free disk space
  - disk space
  - df {{path}}
  - du -sh {{path}}
strategies:
  linux:
    primary: "df -h {{path}}"
  powershell:
    primary: "Get-PSDrive"
  cmd:
    primary: "dir {{path}}"
