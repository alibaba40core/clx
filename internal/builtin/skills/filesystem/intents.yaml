intents:
  - intent: list_dir
    examples:
      - ls
      - la
      - ll
      - dir
      - list directory
      - list directory {{path}}
      - ls {{path}}
      - ll {{path}}
      - dir {{path}}
    strategies:
      linux:
        primary: "ls -la {{path}}"
      powershell:
        argv:
          - Get-ChildItem
          - {{path}}
      cmd:
        primary: "dir {{path}}"
